const authenticateUser = async (req: Request, res: Response) => {
    //if user exsists in db then send back token for jtw session
    //else create new user and then send back token for jtw session
    //TODO LOOK INTO NEXTJS AUTH account , user, etc, how am I going to send user id to query db etc
}
